*** every vaildate lines should like this
*** username   group_address   location   category  anonymous_flag   wait_number
*** values are separated by a TAB('\t'), No TAB in the end of line, anonymous_flag only takes True(1),False(0) as vaildate value
*** wait_number is a '7 chars width' int number
*** wait_queue is in UTF-8 format
